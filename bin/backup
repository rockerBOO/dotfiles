#!/usr/bin/env zsh

to="/Volumes/Backup/Current"

directories=(
  Code
  Documents
  Movies
  Music
  Pictures
)

for directory in ${directories[@]} 
do
    rsync -avh --progress --stats --delete ~/$directory "$to"
done
